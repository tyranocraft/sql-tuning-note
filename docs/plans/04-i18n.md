# Task 4: i18n 설정

> **공통 컨텍스트:** [00-overview.md](00-overview.md)를 먼저 읽을 것.
> **의존성:**
> - Task 2 ([02-content-types.md](02-content-types.md)): `src/lib/types.ts` (`Locale`, `Dictionary`)

**파일:**
- 생성: `src/dictionaries/ko.json`
- 생성: `src/dictionaries/en.json`
- 생성: `src/lib/i18n.ts`
- 생성: `src/middleware.ts`

- [ ] **Step 1: 한국어 사전 생성**

`src/dictionaries/ko.json` 생성:

```json
{
  "site": {
    "title": "SQL Tuning Note",
    "description": "SQL 쿼리 성능 개선을 위한 이론과 실습"
  },
  "nav": {
    "home": "홈",
    "notes": "학습하기"
  },
  "landing": {
    "hero": {
      "title": "SQL 쿼리 성능,\n제대로 이해하고 개선하자",
      "subtitle": "인덱스 동작원리부터 실행계획 분석까지, 실무에서 바로 쓰는 SQL 튜닝 가이드",
      "cta": "학습 시작하기"
    },
    "difficulty": {
      "beginner": "초급",
      "intermediate": "중급",
      "advanced": "고급"
    }
  },
  "content": {
    "prerequisites": "선수 지식",
    "nextSteps": "다음 단계",
    "showHint": "힌트 보기",
    "hideHint": "힌트 접기",
    "showAnswer": "정답 보기",
    "hideAnswer": "정답 접기",
    "checkAnswer": "정답 확인",
    "correct": "정답",
    "incorrect": "오답"
  },
  "search": {
    "placeholder": "주제 검색...",
    "noResults": "검색 결과가 없습니다"
  },
  "filter": {
    "all": "전체",
    "beginner": "초급",
    "intermediate": "중급",
    "advanced": "고급"
  }
}
```

- [ ] **Step 2: 영어 사전 생성**

`src/dictionaries/en.json` 생성:

```json
{
  "site": {
    "title": "SQL Tuning Note",
    "description": "Theory and practice for SQL query performance optimization"
  },
  "nav": {
    "home": "Home",
    "notes": "Learn"
  },
  "landing": {
    "hero": {
      "title": "SQL Query Performance,\nUnderstand and Optimize",
      "subtitle": "From index fundamentals to execution plan analysis — a practical SQL tuning guide",
      "cta": "Start Learning"
    },
    "difficulty": {
      "beginner": "Beginner",
      "intermediate": "Intermediate",
      "advanced": "Advanced"
    }
  },
  "content": {
    "prerequisites": "Prerequisites",
    "nextSteps": "Next Steps",
    "showHint": "Show Hint",
    "hideHint": "Hide Hint",
    "showAnswer": "Show Answer",
    "hideAnswer": "Hide Answer",
    "checkAnswer": "Check Answer",
    "correct": "Correct",
    "incorrect": "Incorrect"
  },
  "search": {
    "placeholder": "Search topics...",
    "noResults": "No results found"
  },
  "filter": {
    "all": "All",
    "beginner": "Beginner",
    "intermediate": "Intermediate",
    "advanced": "Advanced"
  }
}
```

- [ ] **Step 3: i18n 유틸리티 생성**

`src/lib/i18n.ts` 생성:

```ts
import type { Locale, Dictionary } from "./types";

const dictionaries: Record<Locale, () => Promise<Dictionary>> = {
  ko: () => import("@/dictionaries/ko.json").then((m) => m.default as Dictionary),
  en: () => import("@/dictionaries/en.json").then((m) => m.default as Dictionary),
};

export const locales: Locale[] = ["ko", "en"];
export const defaultLocale: Locale = "ko";

export function getDictionary(locale: Locale): Promise<Dictionary> {
  return dictionaries[locale]();
}

export function isValidLocale(value: string): value is Locale {
  return locales.includes(value as Locale);
}
```

- [ ] **Step 4: 로케일 미들웨어 생성**

`src/middleware.ts` 생성:

```ts
import { NextRequest, NextResponse } from "next/server";

const locales = ["ko", "en"];
const defaultLocale = "ko";

export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  const pathnameHasLocale = locales.some(
    (locale) =>
      pathname.startsWith(`/${locale}/`) || pathname === `/${locale}`
  );

  if (pathnameHasLocale) return;

  request.nextUrl.pathname = `/${defaultLocale}${pathname}`;
  return NextResponse.redirect(request.nextUrl);
}

export const config = {
  matcher: ["/((?!_next|api|favicon.ico|.*\\..*).*)"],
};
```

- [ ] **Step 5: 미들웨어 동작 확인**

```bash
npm run dev
```

http://localhost:3000 접속 시 http://localhost:3000/ko 로 리다이렉트되는지 확인한다.
개발 서버를 중지한다.

